extends layout

block content
  h1= title
   var image_names=["image 1.jpg","image 2.jpg","image 3.jpg","image 4.jpg","image 5.jpg"]
   for(let i=0;i<3;i++){
      let random= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[random]}` alt=`Image No ${random}` height="300px" width="300px")

   }